---
description: 代码检查规则：避免过度设计，改完后整体复查
alwaysApply: true
---

# 代码检查规则

写代码、修 bug、做重构时遵循下面两条：实现要简单够用；做完再整体检查一遍。

## 1. 不要过度设计

优先选**简单、直接、够用**的实现；不要为尚未存在的需求提前抽象。

- 先解决当前问题，再考虑扩展；没有第二个调用方就不要抽通用层
- 能用固定间隔 / 简单分支说清的，不要上状态机、策略模式、指数退避框架
- 控制流用普通 `for` / `if` / early return；避免标签 `goto`、多余 flag、多层嵌套「框架感」结构
- 配置项、Option、接口抽象只在已有明确多变体需求时引入
- 日志与注释说明「做了什么」，不要用复杂机制表达简单意图

```go
// ❌ 过度：指数退避、重置、标签 break、多余状态
const backoffMin, backoffMax = time.Second, 30 * time.Second
backoff := backoffMin
// needReconnect + consume: + backoff *= 2 ...

// ✅ 够用：通道关了等 1s 再订
_ = sub.Close()
select {
case <-ctx.Done():
	return
case <-time.After(time.Second):
}
```

自检：删掉一半结构后，行为是否仍正确且更易读？若是，就删。

## 2. 处理完再整体检查

修 bug、改逻辑、加功能后，**不要立刻收工**。宣告完成前通读改动与调用链，确认没有潜在或遗漏问题。

检查：

- 主路径、错误路径、退出/取消路径是否都走通
- 是否引入新的 nil panic、泄漏、竞态、静默吞错、行为回退
- 相邻同模式代码是否有同样问题
- 复查是找漏洞，不是借机加抽象（与上一节一致）

做法：

1. 改完先自测或至少静态通读 diff
2. 从入口跟到出口看一遍（含 defer / Close / ctx.Done）
3. 若发现新问题：先修再结束，不要只记在口头上

自检：若只能回答「主流程好像好了」，还不够；要能回答「异常和边界我也看过了」。
